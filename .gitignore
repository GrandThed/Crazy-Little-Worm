# --- Roblox / Rojo ---
# Studio lock files (created while a place is open)
*.rbxl.lock
*.rbxlx.lock
# Rojo sourcemap (generated, per-machine)
sourcemap.json

# --- Wally packages (restored from wally.toml; wally.lock IS committed) ---
game/Packages/
game/ServerPackages/
game/DevPackages/

# --- Blender backups ---
*.blend1
*.blend2

# --- OS / editor noise ---
Thumbs.db
Desktop.ini
.DS_Store
